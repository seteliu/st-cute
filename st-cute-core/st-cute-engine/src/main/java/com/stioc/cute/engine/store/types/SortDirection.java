package com.stioc.cute.engine.store.types;

/**
 * 存储查询排序方向枚举（纯 Java 内核契约，零外部依赖）。
 */
public enum SortDirection {

    /**
     * 升序排列 (ASC)
     */
    ASC,

    /**
     * 降序排列 (DESC)
     */
    DESC
}
